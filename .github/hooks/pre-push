#!/bin/bash
# .git/hooks/pre-push

echo "🔨 Gradle 빌드 검증 (push 전 실행)…"

if ! ./gradlew clean build --no-daemon; then
  echo "❌ 빌드 실패—푸시 중단"
  exit 1
fi

echo "✅ 빌드 성공—푸시 진행"
